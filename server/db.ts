// Local file-based storage - no external database needed
export const db = {
  // This file is now just a placeholder since we use local JSON files
  // All database operations are handled in storage.ts
};